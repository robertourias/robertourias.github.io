# GitHub personal access token — necessário para buscar dados do repositório
# via API (evita rate limit de 60 req/h para requests sem autenticação).
# Gere em: https://github.com/settings/tokens (escopo: public_repo, read:packages)
GITHUB_TOKEN=
